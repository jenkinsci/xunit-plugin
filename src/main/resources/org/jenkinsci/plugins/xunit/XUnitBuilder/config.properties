reportType.title=Report Type
thresholds.title=Thresholds
thresholdMode.title=Choose your threshold mode
thresholdMode.count=Use a number of tests
thresholdMode.percentage=Use a percent of tests
extraConfiguration.title=Extra Configuration
extraConfiguration.description=Give the report time margin value in ms (default to 3000), before to fail if not new (unless the option ''Fail the build if test results were not updated this run'' is checked).